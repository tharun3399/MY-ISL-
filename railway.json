{
  "build": {
    "builder": "nixpacks",
    "buildCommand": "npm install && cd frontend && npm install && npm run build && cd ../backend/express/expressapp && npm install"
  },
  "deploy": {
    "startCommand": "npm run start:prod"
  }
}
